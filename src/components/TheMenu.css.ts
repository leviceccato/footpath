import { style } from '@vanilla-extract/css'

export const root = style({
	display: 'flex',
	alignItems: 'center',
	gap: 20,
	padding: '0 20px',
})

export const i18nIcon = style({
	minWidth: 20,
	width: 20,
	height: 20,
})
